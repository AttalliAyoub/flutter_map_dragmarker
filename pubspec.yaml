name: flutter_map_dragmarker
description: Dragmarker class for flutter_map null safety

version: 1.0.0+2

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # flutter_map: ^0.13.1
  flutter_map: ">=0.13.1"
  latlong2: any

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true